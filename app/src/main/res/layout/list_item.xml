<?xml version="1.0" encoding="utf-8"?>

<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="cityListModel"
            type="com.dennisiluma.openweathermap.model.CityListModel" />
    </data>

    <androidx.cardview.widget.CardView
        xmlns:cardview="http://schemas.android.com/apk/res-auto"
        android:id="@+id/cardview_id"
        android:layout_width="@dimen/dp_150"
        android:layout_height="@dimen/dp_190"
        android:layout_margin="@dimen/dp_5"
        android:clickable="true"
        android:focusable="true"
        android:foreground="?android:attr/selectableItemBackground"
        cardview:cardCornerRadius="@dimen/dp_4">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical">

            <ImageView
                android:id="@+id/country_image"
                imageUrl="@{cityListModel.image}"
                android:layout_width="match_parent"
                android:layout_height="@dimen/dp_160"
                android:scaleType="centerCrop" />

            <TextView
                android:id="@+id/country_name"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:gravity="center"
                android:text="@{cityListModel.name, default=Nothing}"
                android:textColor="@color/color_2d2d2d"
                android:textSize="@dimen/sp_13" />


        </LinearLayout>

    </androidx.cardview.widget.CardView>

</layout>